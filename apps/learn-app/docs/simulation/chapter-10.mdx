# Chapter 10: Introduction to Robot Simulation and Gazebo

## Learning Objectives

- Understand the importance of simulation in robotics
- Set up Gazebo simulator with ROS 2
- Create robot models with URDF/SDF
- Simulate sensors (LiDAR, cameras, IMU)
- Understand the sim-to-real transfer challenge

## Prerequisites

- Chapters 1-9 (Foundations and ROS 2 complete)
- Understanding of 3D geometry and physics

## Introduction

Simulation accelerates robotics development by enabling rapid prototyping, safe testing, and parallel training without hardware constraints or safety risks. This chapter introduces Gazebo, the most widely used robotics simulator.

## Core Concepts

### 1. Why Simulation?

**Benefits**:
- 🚀 **Speed**: Train 10-100x faster than real-time
- 💰 **Cost**: No hardware wear and tear
- 🛡️ **Safety**: Test dangerous scenarios
- 📊 **Scale**: Run thousands of parallel simulations
- 🔁 **Reproducibility**: Exact scenario replay

**Challenges**:
- 🎯 **Sim-to-Real Gap**: Simulated physics ≠ real physics
- 📐 **Modeling Accuracy**: Sensor noise, friction, dynamics
- 💻 **Computational Cost**: High-fidelity simulation is expensive

### 2. Gazebo Architecture

```
┌─────────────────────────────────────────┐
│         Gazebo Simulator                │
├─────────────────────────────────────────┤
│  World       │  Physics  │  Rendering   │
│  (SDF)       │  (ODE)    │  (OGRE)      │
├──────────────┴───────────┴──────────────┤
│         ROS 2 Integration                │
│       (gazebo_ros_pkgs)                  │
└─────────────────────────────────────────┘
         ↕                  ↕
    ROS 2 Topics      ROS 2 Services
```

### 3. URDF Robot Model

```xml
<?xml version="1.0"?>
<robot name="simple_robot">
  <!-- Base Link -->
  <link name="base_link">
    <visual>
      <geometry>
        <box size="0.6 0.4 0.2"/>
      </geometry>
      <material name="blue">
        <color rgba="0 0 0.8 1"/>
      </material>
    </visual>

    <collision>
      <geometry>
        <box size="0.6 0.4 0.2"/>
      </geometry>
    </collision>

    <inertial>
      <mass value="10"/>
      <inertia ixx="0.1" ixy="0.0" ixz="0.0"
               iyy="0.1" iyz="0.0" izz="0.1"/>
    </inertial>
  </link>

  <!-- Wheel Link -->
  <link name="left_wheel">
    <visual>
      <geometry>
        <cylinder radius="0.1" length="0.05"/>
      </geometry>
      <material name="black">
        <color rgba="0 0 0 1"/>
      </material>
    </visual>

    <collision>
      <geometry>
        <cylinder radius="0.1" length="0.05"/>
      </geometry>
    </collision>

    <inertial>
      <mass value="1"/>
      <inertia ixx="0.01" ixy="0" ixz="0"
               iyy="0.01" iyz="0" izz="0.01"/>
    </inertial>
  </link>

  <!-- Joint -->
  <joint name="left_wheel_joint" type="continuous">
    <parent link="base_link"/>
    <child link="left_wheel"/>
    <origin xyz="-0.2 0.25 -0.1" rpy="-1.5708 0 0"/>
    <axis xyz="0 0 1"/>
  </joint>
</robot>
```

### 4. Gazebo Plugins for Sensors

```xml
<!-- Add to robot URDF -->
<gazebo reference="laser_link">
  <sensor name="laser" type="ray">
    <pose>0 0 0 0 0 0</pose>
    <visualize>true</visualize>
    <update_rate>10</update_rate>
    <ray>
      <scan>
        <horizontal>
          <samples>360</samples>
          <resolution>1</resolution>
          <min_angle>-3.14</min_angle>
          <max_angle>3.14</max_angle>
        </horizontal>
      </scan>
      <range>
        <min>0.1</min>
        <max>10.0</max>
      </range>
    </ray>

    <plugin name="laser_controller" filename="libgazebo_ros_ray_sensor.so">
      <ros>
        <namespace>/robot</namespace>
        <remapping>~/out:=scan</remapping>
      </ros>
      <output_type>sensor_msgs/LaserScan</output_type>
      <frame_name>laser_link</frame_name>
    </plugin>
  </sensor>
</gazebo>
```

## Hands-On Tutorial: Differential Drive Robot in Gazebo

**1. Create robot package**:

```bash
ros2 pkg create --build-type ament_cmake my_robot_description
cd my_robot_description
mkdir urdf launch worlds
```

**2. Robot URDF** (`urdf/diff_drive_robot.urdf.xacro`):

```xml
<?xml version="1.0"?>
<robot xmlns:xacro="http://www.ros.org/wiki/xacro" name="diff_drive_robot">

  <xacro:property name="wheel_radius" value="0.1"/>
  <xacro:property name="wheel_width" value="0.05"/>

  <!-- Include Gazebo differential drive plugin -->
  <gazebo>
    <plugin name="diff_drive" filename="libgazebo_ros_diff_drive.so">
      <ros>
        <namespace>/robot</namespace>
      </ros>

      <update_rate>30</update_rate>
      <left_joint>left_wheel_joint</left_joint>
      <right_joint>right_wheel_joint</right_joint>

      <wheel_separation>0.5</wheel_separation>
      <wheel_diameter>${2 * wheel_radius}</wheel_diameter>

      <max_wheel_torque>20</max_wheel_torque>
      <max_wheel_acceleration>1.0</max_wheel_acceleration>

      <publish_odom>true</publish_odom>
      <publish_odom_tf>true</publish_odom_tf>
      <publish_wheel_tf>true</publish_wheel_tf>

      <odometry_frame>odom</odometry_frame>
      <robot_base_frame>base_link</robot_base_frame>
    </plugin>
  </gazebo>

</robot>
```

**3. Launch file** (`launch/gazebo.launch.py`):

```python
from launch import LaunchDescription
from launch.actions import IncludeLaunchDescription
from launch.launch_description_sources import PythonLaunchDescriptionSource
from launch_ros.actions import Node
import os
from ament_index_python.packages import get_package_share_directory

def generate_launch_description():
    pkg_gazebo_ros = get_package_share_directory('gazebo_ros')
    pkg_robot = get_package_share_directory('my_robot_description')

    # Gazebo launch
    gazebo = IncludeLaunchDescription(
        PythonLaunchDescriptionSource(
            os.path.join(pkg_gazebo_ros, 'launch', 'gazebo.launch.py')
        )
    )

    # Spawn robot
    spawn_entity = Node(
        package='gazebo_ros',
        executable='spawn_entity.py',
        arguments=[
            '-entity', 'diff_drive_robot',
            '-file', os.path.join(pkg_robot, 'urdf', 'diff_drive_robot.urdf'),
            '-x', '0', '-y', '0', '-z', '0.2'
        ]
    )

    return LaunchDescription([
        gazebo,
        spawn_entity
    ])
```

**4. Control the robot**:

```bash
# Launch Gazebo with robot
ros2 launch my_robot_description gazebo.launch.py

# Teleop control
ros2 run teleop_twist_keyboard teleop_twist_keyboard --ros-args -r /cmd_vel:=/robot/cmd_vel
```

## Summary

This chapter introduced robot simulation with Gazebo:

- Benefits and challenges of simulation
- URDF robot modeling
- Gazebo plugins for sensors and actuators
- Launching and controlling simulated robots

Simulation is essential for rapid development and safe testing before deploying to hardware.

## Exercises

1. Add a camera sensor to the differential drive robot
2. Create a simple world with obstacles in SDF format
3. Tune the differential drive plugin parameters to match real robot dynamics

## Next Chapter

Chapter 11 covers advanced Gazebo features: custom worlds, sensor simulation, and physics tuning.

---

**Chapter Progress**: 10 of 26 | **Part**: Simulation (3/7)
